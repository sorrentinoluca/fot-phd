A. Sintesi (processo e stato)

Il ciclo è: correzione locale → consegna → verifica indipendente → acquisizione del NON OK → correzione successiva, su worktree isolati con provenienza e hash. Ha prodotto cinque candidati (59b6b93 → 0c8157f → 9e18bcb → edb37f3 → a219bd4), catena git lineare. Dopo i primi due giri (R01–R10, poi residui su conformità alternativa/N48/contatore producer), dal terzo giro in poi tutti i rilievi (D01, D02, D03) colpiscono un solo invariante: dopo un restart, la riconferma di uno stadio già chiuso deve ri-autenticare l'intera catena dei predecessori e ogni prova durevole esattamente come la chiusura originale. Ogni correzione ha rinforzato una sotto-verifica di quell'invariante (edb37f3 aggiunge _prerequisites; a219bd4 aggiunge _validate_outcome/_closed_outcome/_validate_attempts), lasciando scoperta la successiva. D03 è l'ultima scheggia: la riconferma della prova zero-token di un antenato di retry non ricontrolla il contenuto (token=0) e non ha un digest ricalcolabile che leghi il contenuto persistito, quindi accetta una prova alterata con token positivi. Le suite verdi non l'hanno intercettato perché codice e test condividono lo stesso autore e lo stesso modello (gpt-6-astra sia in preparazione, xhigh, sia in review, high: model_diversity: false), e la copertura è enumerata per caso di manomissione, non per invariante. Il candidato è tecnicamente NON OK per D03; il "Guardiano documentale NON PASS" e ogni NO-GO scientifico sono cose diverse (§sotto). Nessun freeze, nessun GO.

Tre "NON OK" da non confondere:

NON OK tecnico del candidato = D03 (difetto di ledger.py, verificabile offline). È l'unico bloccante attuale del ciclo harness.
Guardiano documentale NON PASS = docs/test_explanation.py: 35 test, gli stessi 14 fallimenti preesistenti dei walkthrough v1 (MAINTENANCE §5 fissa la baseline a 14 al 2026-09-11, criterio = "non peggiorato", non "zero"). Non è introdotto dall'harness e non è il motivo del NON OK tecnico. Trattarlo come bloccante è un errore di categoria.
NO-GO scientifico / autorizzazione pilot = D9, ordine label reale, 16 insight reali, tokenizer/capienza, qualificazione servizi, T5 (non misurabile offline), sonda/gate reali. Esplicitamente fuori perimetro di questa consegna (SPECIFICA §8, CONTRATTO "Confine delle prove"). Un OK tecnico su 03.10 non toccherebbe questi.
B. Tabella delle cause
Causa	Evidenza precisa	Conseguenza	Certezza
Doppio percorso di validazione divergente (acquisizione vs riuso)	reconcile_zero_token (ledger.py) verifica prompt/completion/total_tokens == 0, provider_evidence, disposition; ma _validate_attempts, ramo if row['request_id'] in children, ricontrolla solo disposition, request_id, request_identity_sha256, approval.decision, proof_sha256==artifact_sha256, approval.evidence_sha256==artifact_sha256 — non i token	Una prova zero-token con token positivi passa la riconferma → D03	Dimostrato (codice a219bd4)
Impronta non ricalcolabile sulla prova zero-token	Per l'evento reconciled: l'artifact_sha256 è sha256_file(evidence_path) (hash del file esterno), non digest(evidence). Tutti gli altri artefatti durevoli hanno digest ricalcolabile e ricontrollato: records_sha256 in _closed_outcome, raw/record in _evaluated_record, digest(frozen) in _frozen, digest(record) in _gate_transport_record	Manomettere il contenuto in detail_json è indetectabile: non esiste impronta interna da confrontare. I confronti proof_sha256==artifact_sha256 sono scalari auto-referenziali che una manomissione dei soli token non tocca	Dimostrato (codice)
Correzione guidata dalla riproduzione, non dall'invariante	Comparsa incrementale delle guardie: _prerequisites solo da edb37f3; _validate_outcome/_closed_outcome/_validate_attempts solo da a219bd4 (grep sui 5 commit). Ogni giro chiude il caso ricevuto e ne scopre uno adiacente sullo stesso percorso restart	Whack-a-mole: D01→D02→D03 sono tutti sotto-casi dello stesso invariante di riconferma	Dimostrato (delta commit)
Punto cieco condiviso codice↔test	test_D02_retry_replay_...rejects_missing_ancestor manomette la prova solo cancellando l'evento reconciled:; test_D02_record_digest_binding_... manomette detail_json solo per outcome:*/frozen_gate (che sono digest-bound). Nessun test altera il contenuto dell'evidence reconciled: (token→positivi)	Le suite restano verdi pur col difetto presente	Dimostrato (test a219bd4)
Assenza di diversità di modello nella verifica	runtime.json: preparatore gpt-6-astra/xhigh; revisore precedente gpt-6-astra/high; model_diversity: false. Viola lo spirito di Verifica_LLM.md ("stesso modello ⇒ è una rilettura")	Preparatore e revisore condividono il modello mentale: il revisore trova un caso per giro invece di generalizzare; il preparatore ripete il punto cieco nei test	Dimostrato (runtime.json) + inferenza sul nesso causale
Confine del modello di guasto ambiguo su un bordo	CONTRATTO §D02: in scope "raw/record alterati, dati assenti, copertura ridotta, prova di retry perduta"; fuori "riscrittura coerente dell'intero database e tutte le impronte". D03 è manomissione a campo singolo senza riscrittura coerente	Il preparatore non può derubricare D03 come fuori scope: rientra nella corruzione rilevabile dichiarata	Dimostrato (CONTRATTO) + giudizio

Nota di classificazione (Domanda 1): giro 1 = difetti originari (R01–R10). Giro 2 = misto: difetti preesistenti + una rettifica di requisito (N48/T3-T6: il CONTRATTO ammette "la formulazione precedente su N48 era errata"). Giri 3–5 (D01/D02/D03) = correzioni incomplete progressivamente scoperte sullo stesso invariante, non regressioni di comportamento prima funzionante (quel percorso di riconferma non esisteva prima) e non spostamento del criterio da parte del revisore (l'invariante è stabile).

C. Miglioramenti prioritari (max 5)

1. [CODICE] Legare la prova zero-token con un digest ricalcolabile e ricontrollarlo ovunque.
Cosa: memorizzare, dentro la catena durevole, digest(canonical_json(evidence)) (non solo l'hash del file) e includere in _validate_attempts/_closed_outcome sia il ricalcolo del digest sia i controlli di contenuto identici a reconcile_zero_token (i tre token = 0, provider_evidence, disposition). Dove: ledger.py (reconcile_zero_token, _validate_attempts, e legame dell'evidence nel records_sha256/artefatto di chiusura). Perché: elimina D03 e, legando il contenuto a un digest, sposta ogni futura manomissione nella zona "riscrittura coerente" già dichiarata fuori scope. Verifica: la manomissione dei token dell'evento reconciled: deve sollevare HarnessError in tutti gli ingressi di conferma (binding/success/outcome/freeze, runner, CLI --resume), a database logico invariato.

2. [CODICE] Un unico validatore realmente unico, per digest.
Cosa: rendere _validate_outcome l'unico luogo che autentica ogni artefatto durevole (record, outcome, frozen, evidence zero-token, retry) confrontando sempre un digest ricalcolato con quello legato alla chiusura; nessun percorso di riuso deve avere una lista di campi propria. Dove: ledger.py. Perché: la divergenza tra liste di controllo è la causa-radice di D01/D02/D03. Verifica: test di proprietà (vedi #4) che dimostri l'identità insiemistica dei controlli fra chiusura e riconferma.

3. [TEST] Test per invariante, generativo, non per caso.
Cosa: un test che, su un ledger valido, enumeri e manometta ogni campo di ogni riga/evento durevole (tabelle requests, responses, events incl. reconciled:, outcome:, frozen_gate) — incluse alterazioni "coerenti a hash locale ricalcolato" — e asserisca il rifiuto in ogni ingresso di conferma. Dove: nuovo test_*_tamper_matrix.py. Perché: converte il whack-a-mole in un insieme chiuso; avrebbe intercettato D03. Verifica: il test fallisce sul codice a219bd4 attuale e passa solo dopo #1–#2; misurare copertura per campo, non per metodo.

4. [TEST] Fixture negative sulla prova zero-token.
Cosa: in offline_fixtures.py aggiungere costruttori che producono prove zero-token invalide (token positivi, disposition errata, provider_evidence assente) da usare come input negativi, non solo il costruttore valido Trial.zero(). Perché: oggi non esiste alcuna fixture che generi la prova alterata; l'assenza è il punto cieco. Verifica: ogni variante negativa è rifiutata sia in acquisizione sia in riconferma.

5. [PROCESSO] Ripristinare la diversità di modello e chiudere per invariante.
Cosa: (a) eseguire la verifica indipendente con una famiglia di modello diversa dal preparatore (o dichiarare esplicitamente il limite e compensarlo con revisione umana mirata); (b) il verbale che apre un difetto deve dichiarare l'invariante violato, non solo la riproduzione, e chiedere che la correzione dimostri la chiusura dell'invariante (test #3), non del singolo caso. Dove: Verifica_LLM.md (criterio d'uscita), consegna. Perché: model_diversity:false spiega il punto cieco condiviso; la richiesta per-invariante impedisce correzioni parziali. Verifica: prossimo verbale con modello diverso osservato in runtime.json e con test d'invariante allegato verde.

D. Piano per il prossimo ciclo

Criteri d'ingresso (prima di scrivere codice):

Enunciare per iscritto l'invariante unico: "la riconferma dopo restart autentica esattamente ciò che autentica la chiusura, confrontando digest ricalcolati di ogni artefatto durevole; nessun percorso di riuso ha controlli propri."
Inventario dei campi durevoli (tutte le colonne di requests/responses e tutti i tipi di evento) con, per ciascuno, il digest che lo lega e il punto in cui la riconferma lo ricontrolla. I campi senza digest ricalcolabile (oggi: evidence reconciled:) sono da correggere prima di ogni altra cosa.
Fissare il confine del guasto in una riga operativa: manomissione a campo singolo senza riscrittura di tutte le impronte = in scope, deve fallire; riscrittura coerente di DB + tutte le impronte = fuori scope, dichiarato.

Lavoro: implementare C#1 e C#2; poi C#3 e C#4 (i test devono fallire sul codice attuale, garanzia che colgano il difetto reale); rigenerare la matrice legando ogni riga di test a un campo/artefatto, non a un requisito narrativo.

Criteri d'uscita (verificabili, prima della consegna):

Il test-matrice per campo (#3) è verde e la sua esecuzione sul commit a219bd4 è rossa (prova che non è tautologico).
Nessun percorso di conferma con lista di controlli propria: grep/ispezione mostra un solo validatore; diff di ledger.py limitato agli ingressi di validazione.
Le regressioni storiche D01/D02 restano verdi (non-regressione) e i positivi (replay legittimi, retry a 9 intenti, gate C02 INVALID, remediation) sono preservati.
Perimetro protetto byte-identico; test_explanation.py non peggiorato rispetto ai 14 preesistenti (non usarlo come bloccante).
Verifica indipendente in finestra e famiglia di modello diverse; se impossibile, limite dichiarato + controllo umano sul solo invariante. Verbale con verdetto in prima riga e invariante citato.

Scelta circoscritta vs revisione ampia (Domanda 8): raccomando correzione circoscritta a D03 (C#1) + consolidamento strutturale minimo (C#2/C#3), non una riscrittura generica. Motivo con evidenza: il difetto è localizzato in ledger.py e la sua radice (validatore non-unico + prova senza digest) è dimostrata; una riscrittura ampia introdurrebbe rischio su moduli byte-identici e qualificati (metric_adapter, gate_rules, raccordo metriche) senza evidenza di difetti lì. Il rischio della via circoscritta — un'altra scheggia dello stesso invariante — è neutralizzato proprio dal test per-invariante (#3), che è l'unico modo dimostrabile di chiudere la classe e non il caso.

E. Limiti dell'analisi e materiali mancanti
Verbale D03 non letto direttamente. /Users/luker/fot-tep-riverifica-harness-a219bd4-01a0a1ec/evidence/VERIFICA_D02.md (SHA-256 atteso b88f0465…) e il relativo SHA256SUMS (4ed360ee…) sono fuori dalla cartella collegata; la richiesta d'accesso è scaduta due volte senza risposta. Non ho quindi verificato l'SHA-256 dei due file né riletto la formulazione esatta del revisore su D03. Ho ricostruito D03 dal codice (ledger.py a a219bd4), come richiesto dal mandato; la mia descrizione del meccanismo è dimostrata sul sorgente, non desunta dal verbale. Se mi autorizzi le cartelle sorella, verifico gli hash e confronto parola per parola.
Non ho eseguito nulla (sola lettura): la mia affermazione che il test-matrice fallirebbe su a219bd4 è un'inferenza dal codice, da confermare eseguendola nel ciclo di correzione.
Verbali 1–4 non riletti integralmente dalle cartelle esterne; ho letto le loro acquisizioni committate nel candidato (non_ok_*), i report/prompt/matrici e i delta commit. La cronologia dei rilievi fornita nel mandato è coerente con ciò che ho verificato nel codice e nei commit.
Diversità di modello di questa review: è osservabile che preparatore e revisore precedenti erano entrambi gpt-6-astra; questa analisi è svolta in una sessione diversa e — per quanto il nome configurato indichi una famiglia diversa (Claude) — non posso provare dall'interno il modello effettivamente servito. Non presumo che un nome dimostri diversità: lo tratto come limite dichiarato.

Non dichiaro chiuso D03, non autorizzo freeze né GO. D9 eseguibile, ordine label, qualificazioni, T5 e pilot restano separati e non toccati.