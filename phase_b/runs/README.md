# Phase B run records

No real inference record exists yet. This directory remains empty until a model
and exact version are selected, the insight library is frozen, and the Phase B
protocol receives explicit approval.

Future entries must conform to `evaluation/run_record.schema.json`. True labels
remain in separate evaluator-side metadata and are never embedded in prompts.
